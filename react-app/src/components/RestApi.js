import React from 'react';

const RestApi = () => {
	/*const { data, loading, errors } = useFetch('http://localhost:5000/graphql');
	const renderTemplate = () => {
		if (loading) return <p>Loading...</p>;
		if (errors)
			return errors.map(item => (
				<ol>
					{item.extensions.code}: {item.message}
				</ol>
			));
		return data.map(item => <ol>{item.lastName}</ol>);
	};*/
	//<ul>{renderTemplate()}</ul>
	return <div>RestApi</div>;
};

export default RestApi;
